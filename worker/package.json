{
    "name": "davidcralph-worker",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "deploy": "wrangler deploy",
        "start": "wrangler dev",
        "dev": "wrangler dev",
        "pretty": "prettier --write ."
    },
    "dependencies": {
        "hono": "^4.12.18"
    },
    "devDependencies": {
        "prettier": "^3.8.3",
        "wrangler": "^4.88.0"
    }
}